import React from 'react';
import { AccessForm } from '../../components/access-form';

const page = () => {
    return (
        <div>
            <AccessForm />
        </div>
    );
};

export default page;